# Minimal Semgrep — SAST on ~/.hawaldar/workspace only. No exploit runtime.
FROM docker.io/library/python:3.12-slim-bookworm
RUN pip install --no-cache-dir "semgrep==1.110.0" \
	&& rm -rf /root/.cache /tmp/*
COPY rules /rules
ENTRYPOINT ["semgrep"]
