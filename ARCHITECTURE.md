# Hawaldar Architecture

This branch does not contain a running application. Product constraints from earlier decisions still apply if the project is rebuilt: recon only, policy is the authority on scope, tools run in Podman, never a host shell.
